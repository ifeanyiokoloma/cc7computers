import Products from "../../components/products/Products";

const Computers = ({ link, linkName, extent }) => {
  return <Products link={link} linkName={linkName} extent={extent} />;
};

export default Computers;
